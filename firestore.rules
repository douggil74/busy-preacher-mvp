rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // TEMPORARY TEST MODE - OPEN ACCESS
    // TODO: Add Firebase Authentication and use production rules
    
    match /prayer_requests/{requestId} {
      // Anyone can read and write (for testing)
      allow read, write: if true;
    }
    
    match /users/{userId} {
      // Anyone can read and write (for testing)
      allow read, write: if true;
    }
  }
}
